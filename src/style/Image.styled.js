import styled from 'styled-components'

const StyledImage = styled.img`
  border: none;

  &.post_img {
    height: 400px;
  }

  &.avatar {
    border-radius: 50%;
    height: 135px;
  }
`

export default StyledImage
